
- turn sound off by default

- add a randomize button to the header.  it randomizes all the activies.  Perhaps it deletes every activity on the screen, respawning a new randomly chosen module of the same size and location.  propose an implementation.

- add a stock price list with random stock tickers/prices, high/low/close, red/green tagging for gain/loss, dynamic price updates

- add a stock graph for a single stock, random ticker, random data, updates every 15 seconds

- Set the default layout to 6 across by four down

- Set the default number of popups to zero

- Slow down the motion of the satellites in the satellite constellation views by 10x

- Change the default linger time for activities to 2 minutes with SD of 15 seconds

- In the Heart Monitor activity, the text readouts below the ekg graph run into each other when in 6x4 layout because they are too large.  It works in 6x8 layout.

